<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed"
        data-toggle="collapse" data-target="#navbar-items" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="/" class="navbar-brand">CSTS</a>
    </div>

    <div class="collapse navbar-collapse" id="navbar-items">
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= 'active' if @page == nil and
          controller_name.eql?('users') %>">
          <%= link_to "Users", users_path %>
        </li>
        <li class="<%= 'active' if @page == 'agents' %>">
          <%= link_to "Agents", users_path(page: "agents") %>
        </li>
        <li class="<%= 'active' if @page == 'customers' %>">
          <%= link_to "Customers", users_path(page: "customers") %>
        </li>
        <li class="<%= active_class('tickets') %>">
          <%= link_to "Tickets", tickets_path %>
        </li>
        <% if user_signed_in? %>
          <li>
            <%= link_to "Sign Out", destroy_user_session_path,
              method: :delete %>
          </li>
        <% else %>
          <li>
            <%= link_to "Sign In", new_user_session_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
