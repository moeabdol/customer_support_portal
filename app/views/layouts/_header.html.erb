<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed"
        data-toggle="collapse" data-target="#navbar-items" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="/" class="navbar-brand">CSTS</a>
    </div>

    <div class="collapse navbar-collapse" id="navbar-items">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <% if policy(User).index? %>
            <li class="<%= 'active' if current_page?("/users") %>">
              <%= link_to "Users", users_path %>
            </li>
          <% end %>

          <% if policy(User).agents? %>
            <li class="<%= 'active' if current_page?("/users/agents") %>">
              <%= link_to "Agents", agents_users_path %>
            </li>
          <% end %>

          <% if policy(User).customers? %>
            <li class="<%= 'active' if current_page?("/users/customers") %>">
              <%= link_to "Customers", customers_users_path %>
          </li>
          <% end %>

          <% if policy(Ticket).index? %>
            <li class="<%= active_class('tickets') %>">
              <%= link_to "Tickets", tickets_path %>
            </li>
          <% end %>

          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"
              role="button" aria-haspopup="true" aria-expanded="false">
              <%= current_user.email %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to "Profile", user_path(current_user) %>
              </li>
              <li>
                <%= link_to "Sign Out", destroy_user_session_path,
                  method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li>
            <%= link_to "Sign In", new_user_session_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
